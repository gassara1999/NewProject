{% extends 'base.html.twig' %}

{% block title %}PrivateCoaching index{% endblock %}

{% block body %}
    <h1>PrivateCoaching index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateSession</th>
                <th>BeginHour</th>
                <th>EndHour</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for private_coaching in private_coachings %}
            <tr>
                <td>{{ private_coaching.id }}</td>
                <td>{{ private_coaching.DateSession ? private_coaching.DateSession|date('Y-m-d') : '' }}</td>
                <td>{{ private_coaching.BeginHour }}</td>
                <td>{{ private_coaching.EndHour }}</td>
                <td>
                    <a href="{{ path('app_private_show', {'id': private_coaching.id}) }}">show</a>
                    <a href="{{ path('app_private_edit', {'id': private_coaching.id}) }}">edit</a>
                    <a href="{{ path('app_membership_delete', {'id': membership.id}) }}">delete</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_private_new') }}">Create new</a>
{% endblock %}
