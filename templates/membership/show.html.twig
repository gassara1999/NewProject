{% extends 'base.html.twig' %}

{% block title %}Membership{% endblock %}

{% block body %}
    <h1>Membership</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membership.id }}</td>
            </tr>
            <tr>
                <th>Begin</th>
                <td>{{ membership.begin ? membership.begin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>End</th>
                <td>{{ membership.end ? membership.end|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ membership.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_membership_index') }}">back to list</a>

    <a href="{{ path('app_membership_edit', {'id': membership.id}) }}">edit</a>

    {{ include('membership/_delete_form.html.twig') }}
{% endblock %}
